# TOOL002 MASTER — 2번 입찰 도구

상태: ACTIVE / TOOL-SPECIFIC SINGLE SOURCE
기준일: 2026-08-30

이 문서는 2번 도구에만 해당하는 영구 규칙을 보관한다. WIC 공통 운영규칙은 `obk369369-spec/20-operational-manual-viewer/WIC_GLOBAL_OPERATING_RULES.md`를 우선하며 공통 규칙을 이 문서에 중복 복제하지 않는다.

## 1. 현재 목표
- 나라장터 등에서 내려받은 공고 원본을 한 화면에 붙여넣는다.
- 공고를 `입찰 / 내부안내 / X`로 자동 분기한다.
- 입찰 대상은 기초금액 등 필수값이 있을 때 입찰 금액을 계산한다.
- 발주기관/수요기관을 중복 제거해 누적한다.
- 현재 단계의 제품형 MVP는 `공고 붙여넣기 → 자동 분기 → 입찰 금액 계산 → 기관 누적`까지다.

## 2. 입력
- 엑셀은 원본 공급원일 뿐 최종 사용 화면이 아니다.
- 사용자는 나라장터 등에서 받은 원본 공고 데이터를 브라우저 화면의 붙여넣기 영역에 그대로 넣을 수 있어야 한다.
- 공고번호, 공고명, 발주기관/공고기관, 수요기관, 기초금액, 게시/마감일시 등 확인 가능한 필드를 자동 매핑한다.
- 같은 데이터를 반복 입력하거나 행별로 수동 판단하도록 만들지 않는다.

## 3. 자동 분기
- 최종 분기 상태는 기본적으로 `입찰 / 내부안내 / X` 3종으로 사용한다.
- `입찰`: 실제 입찰 검토 및 금액 계산 대상으로 갈 수 있는 공고.
- `내부안내`: 직접 입찰은 아니지만 월드의 자료안내/후속접촉 가능성이 있는 공고.
- `X`: 무관·빈 데이터·자동 제외 대상.
- 애매한 공고를 억지로 입찰로 올리지 않는다. 근거가 약하면 내부안내 또는 X로 보수적으로 둔다.
- 무리한 추론·추천으로 상태를 올리지 않는다.

## 4. 입찰 금액 계산
- `입찰`로 분류되고 기초금액 등 계산 필수값이 있을 때만 계산한다.
- 기초금액이 없으면 계산을 강행하지 않고 `기초금액 확인 필요`로 표시한다.
- 계산 영역은 실제 업무에 필요한 보증금액, 하한/상한, 후보금액 등의 현재 검증된 계산구조를 재사용한다.
- 이미 PASS된 계산수식/필드 구조는 변경 이유가 없는 한 재작성·재검증하지 않는다.

## 5. 기관 누적
- 이 도구는 단순 공고 필터뿐 아니라 잠재 발주기관 누적 도구 역할을 한다.
- 기관명, 공고 건수, 최근 공고명, 입찰/내부안내/X 이력을 확인할 수 있어야 한다.
- 동일 기관은 중복 행을 계속 만들지 않고 건수·최근 이력을 누적한다.
- 사용자가 기관 목록을 다시 수동 정리하지 않게 한다.

## 6. 현재 범위 밖 / HOLD
- 연구자 연락처 자동수집, 메일 완전자동발송, DM 자동발송, 외부사이트 무차별 크롤링, 과도한 영업문안 자동생성은 현재 MVP 범위 밖이다.
- 메일 자동화가 필요할 경우 직접 새 스크립트를 반복 제작하기보다 검증된 메일 클라이언트/템플릿/첨부 모듈 재사용을 우선한다.
- 기관 정보 확장은 자동 크롤링을 억지로 붙이기보다 누적 DB와 검증된 외부 모듈을 먼저 활용한다.

## 7. 화면/실동작 기준
- 최종 결과는 브라우저 화면에서 직접 확인 가능한 프로그램 형태여야 한다.
- 대화창 설명, 말로 된 표, 파일명 변경, 보이지 않는 미래 연결만으로 완료 처리하지 않는다.
- 사용자는 가능한 한 `붙여넣기 1회 + 확인 1회` 수준으로 사용해야 한다.
- 입력 후 자동 분기·계산·기관 누적 중 실제 기능이 화면에서 동작하지 않으면 완료가 아니다.

## 8. 반복오류 및 재사용
- 이미 검증된 PASS 계산/분류/필드 구조는 SKIP_REUSE한다.
- 새 변경 범위만 FIRST_VALIDATION 1회 수행한다.
- 같은 입력·같은 오류·같은 조건의 반복 재시험을 금지한다.
- 다른 도구에서 검증된 모듈은 재사용할 수 있지만 대화창 간 암묵적 직접연결을 전제로 하지 않는다.
- 껍데기 확장을 막기 위해 현재 MVP 밖 기능을 한 번에 크게 붙이지 않는다.

## 9. 완료 조건
- 원본 공고 붙여넣기 가능.
- 공고가 입찰/내부안내/X로 자동 분기.
- 계산가능 입찰 공고는 금액 계산.
- 계산 불가 사유는 명확히 HOLD/확인 필요 표시.
- 기관명이 중복 없이 누적.
- 사용자가 공고 하나씩 수동 판정하지 않아도 됨.

## 10. 2026-08-30 대화기록 catch-up
- 사용자가 제공한 2번 관련 과거 대화기록의 반복 규칙을 위와 같이 최신 상태로 통합했다.
- 과거 엑셀 단계의 임시 수식·일일 기준점·낡은 PASS/X 표현은 현재 브라우저 MVP 목적과 충돌하면 운영원칙으로 복원하지 않는다.
- 이후 2번 대화창에서 사용자가 `업데이트`라고 입력하면 직전까지의 신규 영구 피드백을 이 MASTER와 대조해 DIFF ONLY 반영한다.
- 동일 규칙은 SKIP_REUSE하고 같은 범위의 충돌은 더 최신의 명시적 사용자 지시를 우선한다.
- 실제 GitHub write/commit/read-back이 없으면 업데이트 완료라고 보고하지 않는다.
